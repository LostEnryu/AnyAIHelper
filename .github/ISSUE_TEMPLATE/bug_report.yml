name: 🐞 バグ報告
description: 不具合を発見した場合はこちらから報告してください。
title: "[BUG] "
labels: ["bug"]
assignees: ["${{ github.actor }}"]
# projects:
#   - vars.PROJECT_NAME
projects: []

body:
  - type: markdown
    attributes:
      value: |
        ## 🐞 不具合報告フォーム
        再現手順や発生環境をできるだけ詳しく記載してください。

  - type: input
    id: summary
    attributes:
      label: 概要
      placeholder: 例）フォーム送信後にページがリロードされない

  - type: textarea
    id: steps
    attributes:
      label: 再現手順
      placeholder: |
        1. 入力フォームを開く  
        2. 必須項目を入力  
        3. 送信ボタンをクリック

  - type: textarea
    id: expected
    attributes:
      label: 期待される動作
      placeholder: 例）送信完了メッセージが表示される

  - type: textarea
    id: actual
    attributes:
      label: 実際の動作
      placeholder: 例）送信ボタンが無反応

  - type: dropdown
    id: severity
    attributes:
      label: 影響度
      options:
        - 致命的（利用不可）
        - 高（主要機能が影響）
        - 中（部分的に影響）
        - 低（軽微な問題）

  - type: input
    id: environment
    attributes:
      label: 環境
      placeholder: "例）Windows 11 / Chrome 118 / Backend: FastAPI 0.111"
